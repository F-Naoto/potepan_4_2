<h1>index</h1>
<%= link_to 'ログイン', new_session_path %>

<%# エリアで検索 %>
<%= form_with(url:area_search_rooms_path, scope: :search, method: :get, local:true) do |f| %>
  <%= f.text_field :area, placeholder:"エリア" %>
<% end %>

<%# キーワードで検索 %>
<%= form_with(url:key_search_rooms_path, scope: :search, method: :get, local: true)  do |f| %>
  <%= f.text_field :area, placeholder:"キーワード" %>
<% end %>

<p>Potepan Shareでまだ体験したことのない旅をしよう</p>
<%# エリアで検索 %>
<%= form_with(url:area_search_rooms_path, scope: :search, method: :get, local:true) do |f| %>
  <%= f.text_field :area, placeholder:"エリア" %>
<% end %>

<%# 画像をクリックするとルームが曖昧検索で表示される %>
<h2>おすすめのエリア</h2>
<div class="area_reccommendation">
<div>
<%= form_with(url:area_search_rooms_path, scope: :search, method: :get, local:true) do |f| %>
  <%= f.hidden_field :area, value:"東京" %>
  <%= f.submit '東京', disable_with: 'Submiting...' %>
<% end %>
</div>
<div>
<%= form_with(url:area_search_rooms_path, scope: :search, method: :get, local:true) do |f| %>
  <%= f.hidden_field :area, value:"大阪" %>
  <%= f.submit '大阪', disable_with: 'Submiting...' %>
<% end %>
</div><div>
<%= form_with(url:area_search_rooms_path, scope: :search, method: :get, local:true) do |f| %>
  <%= f.hidden_field :area, value:"京都" %>
  <%= f.submit '京都', disable_with: 'Submiting...' %>
<% end %>
</div><div>
<%= form_with(url:area_search_rooms_path, scope: :search, method: :get, local:true) do |f| %>
  <%= f.hidden_field :area, value:"札幌" %>
  <%= f.submit '札幌', disable_with: 'Submiting...' %>
<% end %>
</div>
</div>